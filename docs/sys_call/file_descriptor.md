# File Descriptor

A file descriptor is a process unique indentifier for a file or other input/output resource such as pipe or network socket.

They have non negative integer value with negatives reserved for error values.

The `File descriptor` values also symbolises which standard stream are they related to

- `0` - meaning `standard input`
- `1` - meaning `standard output`
- `2` - meaning `standard error`
